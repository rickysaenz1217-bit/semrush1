# Driver Frontend (Vite + React)

This is a minimal React frontend scaffold using Vite.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server runs on http://localhost:3000 by default and can talk to the backend at http://127.0.0.1:8000/api/.
